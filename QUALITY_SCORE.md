# 工程质量基线

更新时间：2026-08-14

| 领域 | 当前等级 | 证据 | 下一门槛 |
|---|---:|---|---|
| 本地数据与隐私边界 | A- | Tauri/Rust 文件边界、SQLite、Keychain、显式在线数据范围 | 诊断包脱敏自动测试 |
| Artifact 与缓存 | A- | 内容哈希、局部复用、统一文件/目录 staging、非空校验、发布失败回滚 | 为所有历史缓存补离线完整性扫描 |
| 任务恢复 | A- | 真实节点接入 run/node/event，跨重启恢复、幂等跳过和外部导出完成测试 | 为 Provider 请求补稳定幂等键 |
| 工作流编排 | A- | 版本化生产 DAG 成为唯一队列控制面，快速/审核模式共享定义 | 将长耗时节点进一步拆成可恢复子单元 |
| 模块边界 | C+ | workflow/application/infrastructure/feature hook 已分层；遗留算法仍集中于 `commands.rs` | 继续迁移 TTS Provider 算法到 application services |
| IPC 合同 | C+ | 阶段已类型化且数据库边界严格解析，Rust/TS 仍手工重复 | 从共享 schema 生成合同 |
| 后端验证 | A- | Runner、SQLite、原子发布、真实 FFmpeg 样片和 Provider 合同均有验证 | 增加带录制响应的 ASR/翻译/TTS E2E |
| 前端验证 | C+ | 阶段分发门禁、类型/构建验证、桌面壳主路由交互冒烟 | 增加可自动化的 Tauri IPC 交互夹具 |
| 可观测性 | B+ | 真实节点记录资源等待、attempt、耗时和结果事件，不记录凭据 | 诊断包展示按节点聚合的延迟与错误率 |
| Runtime 供应链 | C- | 能检测本机组件，清单仍硬编码/待校验 | 签名 manifest、SHA、许可证门禁 |

等级不是发布结论，而是持续收紧的工程导航。架构门禁采用棘轮策略：遗留大文件先禁止继续增长，新模块立即遵守目标边界，完成迁移后降低白名单上限。
